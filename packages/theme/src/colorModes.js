import palette from './palette';

const colorModes = {
  default: {
    background: palette.white,
    muted: palette.pink,
    text: palette.black,
  },
  options: {
    blue: {
      background: palette.blue,
      muted: palette.pink,
      text: palette.white,
    },
  },
};

export default colorModes;
