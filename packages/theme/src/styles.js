import palette from './palette';

const heading = {
  margin: 0,
};

const styles = {
  root: {
    bg: palette.blue,
  },
  h1: {
    ...heading,
  },
  h2: {
    ...heading,
  },
  h3: {
    ...heading,
  },
  p: {
    margin: 0,
  },
};

export default styles;
